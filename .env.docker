# Copy to .env.docker and fill secrets locally; do not commit real API keys.
# Docker Compose loads this file for the backend service.
DB_HOST=db
DB_PORT=5432
DB_USER=user
DB_PASSWORD=userpassword
DB_NAME=apisec
# Optional: set in Dashboard / local only (never commit a live key)
GEMINI_API_KEY=
