# Example environment for `docker compose`. Copy to `.env.docker` and set values.
# Never commit files that contain production passwords or API keys.

DB_HOST=db
DB_PORT=5432
DB_USER=user
DB_PASSWORD=changeme
DB_NAME=apisec
GEMINI_API_KEY=
