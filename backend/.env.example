# Local backend (non-Docker). Copy to `.env` in this folder — `backend/.env` is gitignored.
DB_HOST=localhost
DB_PORT=5432
DB_USER=user
DB_PASSWORD=changeme
DB_NAME=apisec
GEMINI_API_KEY=
JWT_SECRET=generate-a-long-random-string-for-production
